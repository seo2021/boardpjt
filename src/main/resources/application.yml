spring:
  application:
    name: boardpjt
  # profile -> dev, prod
  profiles:
    active: dev
  # 디폴트는 노출을 해놓고, dev는 감춰놓을 정보들을 작성

# 공통사항
  datasource: # JPA
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true